diff --git a/node_modules/cactus-react-native/ios/HybridCactusFileSystem.swift b/node_modules/cactus-react-native/ios/HybridCactusFileSystem.swift
index dd342cc..6f1aa8f 100644
--- a/node_modules/cactus-react-native/ios/HybridCactusFileSystem.swift
+++ b/node_modules/cactus-react-native/ios/HybridCactusFileSystem.swift
@@ -92,8 +92,26 @@ class HybridCactusFileSystem: HybridCactusFileSystemSpec {
         }
       }
 
+      // Foreground .default session. We previously used
+      // URLSessionConfiguration.background so the download survived app
+      // suspension, but iOS throttles background sessions to low-single-digit
+      // Mbps regardless of what the network supports. Keep the app in the
+      // foreground during install; if the user backgrounds mid-download
+      // we'll get NSURLErrorTimedOut (-1001) and the JS-side retry loop
+      // picks up from zero.
       let session = URLSession(configuration: .default, delegate: delegate, delegateQueue: nil)
-      let task = session.downloadTask(with: url)
+
+      // Opt the request into HTTP/3. Safari gets ~300 Mbps on this URL
+      // while NSURLSession.default has been doing 2–3 Mbps — the most
+      // likely culprit is protocol negotiation: Safari negotiates h3,
+      // NSURLSession falls back to a degraded h2/h1.1 path. assumesHTTP3Capable
+      // tells URLSession to try QUIC even without an Alt-Svc advertisement.
+      // See the metrics log below to confirm what actually got negotiated.
+      var request = URLRequest(url: url)
+      if #available(iOS 15.0, *) {
+        request.assumesHTTP3Capable = true
+      }
+      let task = session.downloadTask(with: request)
 
       callback?(0.0)
 
@@ -236,6 +254,25 @@ private class DownloadProgressDelegate: NSObject, URLSessionDownloadDelegate, UR
     }
   }
   
+  // Log negotiated protocol + timing per transaction. Lets us confirm
+  // whether URLSession picked HTTP/3 (h3), h2, or http/1.1 — the most
+  // likely reason app downloads are slower than Safari on the same URL.
+  func urlSession(
+    _ session: URLSession,
+    task: URLSessionTask,
+    didFinishCollecting metrics: URLSessionTaskMetrics
+  ) {
+    for t in metrics.transactionMetrics {
+      let proto = t.networkProtocolName ?? "?"
+      let reused = t.isReusedConnection
+      let remote = t.remoteAddress ?? "?"
+      let status = (t.response as? HTTPURLResponse)?.statusCode ?? -1
+      print(
+        "[cactus.dl.metrics] proto=\(proto) status=\(status) reused=\(reused) remote=\(remote)"
+      )
+    }
+  }
+
   func urlSession(
     _ session: URLSession,
     task: URLSessionTask,
diff --git a/node_modules/cactus-react-native/lib/module/index.js b/node_modules/cactus-react-native/lib/module/index.js
index 9e72096..1c15d98 100644
--- a/node_modules/cactus-react-native/lib/module/index.js
+++ b/node_modules/cactus-react-native/lib/module/index.js
@@ -13,7 +13,7 @@ export { useCactusAudio } from "./hooks/useCactusAudio.js";
 export { useCactusIndex } from "./hooks/useCactusIndex.js";
 
 // Registry
-export { getRegistry } from "./modelRegistry.js";
+export { getRegistry, setModelUrlOverride } from "./modelRegistry.js";
 
 // Types
 //# sourceMappingURL=index.js.map
\ No newline at end of file
diff --git a/node_modules/cactus-react-native/lib/module/modelRegistry.js b/node_modules/cactus-react-native/lib/module/modelRegistry.js
index ab63dd5..0969cda 100644
--- a/node_modules/cactus-react-native/lib/module/modelRegistry.js
+++ b/node_modules/cactus-react-native/lib/module/modelRegistry.js
@@ -1,7 +1,15 @@
 "use strict";
 
-const RUNTIME_VERSION = '1.13.0';
+const RUNTIME_VERSION = '1.14.0';
 let registryPromise = null;
+// Runtime-settable URL overrides, keyed by model slug. App code (typically
+// in __DEV__ builds) can swap a model's download endpoint to a private CDN
+// or local tunnel by calling setModelUrlOverride before lm.download fires.
+const urlOverrides = {};
+export function setModelUrlOverride(slug, override) {
+  urlOverrides[slug] = { ...(urlOverrides[slug] ?? {}), ...override };
+  registryPromise = null;
+}
 export function getRegistry() {
   return registryPromise ??= fetchRegistry();
 }
@@ -43,38 +51,57 @@ async function fetchRegistry() {
     tags = []
   }) => {
     const weights = siblings.map(s => s.rfilename).filter(f => f.startsWith('weights/') && f.endsWith('.zip'));
-    if (!weights.some(f => f.endsWith('-int4.zip')) || !weights.some(f => f.endsWith('-int8.zip'))) return;
+    const hasInt4 = weights.some(f => f.endsWith('-int4.zip'));
+    const hasInt8 = weights.some(f => f.endsWith('-int8.zip'));
+    if (!hasInt4 && !hasInt8) return;
     const version = await resolveWeightVersion(id).catch(() => null);
     if (!version) return;
-    const key = weights.find(f => f.endsWith('-int4.zip')).replace('weights/', '').replace('-int4.zip', '');
+    const sampleWeight = weights.find(f => f.endsWith('-int4.zip')) ?? weights.find(f => f.endsWith('-int8.zip'));
+    const key = sampleWeight.replace('weights/', '').replace(/-int[48]\.zip$/, '');
     const base = `https://huggingface.co/${id}/resolve/${version}/weights/${key}`;
     const tree = await fetch(`https://huggingface.co/api/models/${id}/tree/${version}/weights`).then(r => r.ok ? r.json() : null).catch(() => null);
     const sizeMb = suffix => Math.round((tree?.find(f => f.path === `weights/${key}${suffix}`)?.size ?? 0) / (1024 * 1024));
+    const quantization = {};
+    if (hasInt4) {
+      quantization.int4 = {
+        sizeMb: sizeMb('-int4.zip'),
+        url: `${base}-int4.zip`,
+        ...(weights.some(f => f.endsWith('-int4-apple.zip')) ? {
+          pro: {
+            apple: `${base}-int4-apple.zip`
+          }
+        } : {})
+      };
+    }
+    if (hasInt8) {
+      quantization.int8 = {
+        sizeMb: sizeMb('-int8.zip'),
+        url: `${base}-int8.zip`,
+        ...(weights.some(f => f.endsWith('-int8-apple.zip')) ? {
+          pro: {
+            apple: `${base}-int8-apple.zip`
+          }
+        } : {})
+      };
+    }
     registry[key] = {
       slug: key,
       capabilities: tags.filter(t => !t.includes(':')),
-      quantization: {
-        int4: {
-          sizeMb: sizeMb('-int4.zip'),
-          url: `${base}-int4.zip`,
-          ...(weights.some(f => f.endsWith('-int4-apple.zip')) ? {
-            pro: {
-              apple: `${base}-int4-apple.zip`
-            }
-          } : {})
-        },
-        int8: {
-          sizeMb: sizeMb('-int8.zip'),
-          url: `${base}-int8.zip`,
-          ...(weights.some(f => f.endsWith('-int8-apple.zip')) ? {
-            pro: {
-              apple: `${base}-int8-apple.zip`
-            }
-          } : {})
-        }
-      }
+      quantization
     };
   }));
+  // Apply any runtime overrides set via setModelUrlOverride.
+  for (const slug of Object.keys(urlOverrides)) {
+    const entry = registry[slug];
+    const override = urlOverrides[slug];
+    if (!entry || !override) continue;
+    if (override.proApple && entry.quantization?.int4?.pro) {
+      entry.quantization.int4.pro.apple = override.proApple;
+    }
+    if (override.url && entry.quantization?.int4) {
+      entry.quantization.int4.url = override.url;
+    }
+  }
   return registry;
 }
 //# sourceMappingURL=modelRegistry.js.map
\ No newline at end of file
diff --git a/node_modules/cactus-react-native/src/index.tsx b/node_modules/cactus-react-native/src/index.tsx
index adb0a03..6e96e1b 100644
--- a/node_modules/cactus-react-native/src/index.tsx
+++ b/node_modules/cactus-react-native/src/index.tsx
@@ -11,7 +11,7 @@ export { useCactusAudio } from './hooks/useCactusAudio';
 export { useCactusIndex } from './hooks/useCactusIndex';
 
 // Registry
-export { getRegistry } from './modelRegistry';
+export { getRegistry, setModelUrlOverride } from './modelRegistry';
 
 // Types
 export type { CactusModel, CactusModelOptions } from './types/common';
diff --git a/node_modules/cactus-react-native/src/modelRegistry.ts b/node_modules/cactus-react-native/src/modelRegistry.ts
index 02197f8..3f9dc06 100644
--- a/node_modules/cactus-react-native/src/modelRegistry.ts
+++ b/node_modules/cactus-react-native/src/modelRegistry.ts
@@ -1,9 +1,20 @@
 import type { CactusModel } from './types/common';
 
-const RUNTIME_VERSION = '1.13.0';
+const RUNTIME_VERSION = '1.14.0';
 
 let registryPromise: Promise<{ [key: string]: CactusModel }> | null = null;
 
+// Runtime-settable URL overrides, keyed by model slug. App code (typically
+// in __DEV__ builds) can swap a model's download endpoint to a private CDN
+// or local tunnel by calling setModelUrlOverride before lm.download fires.
+type UrlOverride = { proApple?: string; url?: string };
+const urlOverrides: { [slug: string]: UrlOverride } = {};
+
+export function setModelUrlOverride(slug: string, override: UrlOverride): void {
+  urlOverrides[slug] = { ...(urlOverrides[slug] ?? {}), ...override };
+  registryPromise = null;
+}
+
 export function getRegistry(): Promise<{ [key: string]: CactusModel }> {
   return (registryPromise ??= fetchRegistry());
 }
@@ -64,19 +75,19 @@ async function fetchRegistry(): Promise<{ [key: string]: CactusModel }> {
         .map((s: any) => s.rfilename as string)
         .filter((f) => f.startsWith('weights/') && f.endsWith('.zip'));
 
-      if (
-        !weights.some((f) => f.endsWith('-int4.zip')) ||
-        !weights.some((f) => f.endsWith('-int8.zip'))
-      )
-        return;
+      const hasInt4 = weights.some((f) => f.endsWith('-int4.zip'));
+      const hasInt8 = weights.some((f) => f.endsWith('-int8.zip'));
+      if (!hasInt4 && !hasInt8) return;
 
       const version = await resolveWeightVersion(id).catch(() => null);
       if (!version) return;
 
-      const key = weights
-        .find((f) => f.endsWith('-int4.zip'))!
+      const sampleWeight =
+        weights.find((f) => f.endsWith('-int4.zip')) ??
+        weights.find((f) => f.endsWith('-int8.zip'))!;
+      const key = sampleWeight
         .replace('weights/', '')
-        .replace('-int4.zip', '');
+        .replace(/-int[48]\.zip$/, '');
 
       const base = `https://huggingface.co/${id}/resolve/${version}/weights/${key}`;
 
@@ -94,28 +105,46 @@ async function fetchRegistry(): Promise<{ [key: string]: CactusModel }> {
             (1024 * 1024)
         );
 
+      const quantization: CactusModel['quantization'] = {} as any;
+      if (hasInt4) {
+        quantization.int4 = {
+          sizeMb: sizeMb('-int4.zip'),
+          url: `${base}-int4.zip`,
+          ...(weights.some((f) => f.endsWith('-int4-apple.zip'))
+            ? { pro: { apple: `${base}-int4-apple.zip` } }
+            : {}),
+        };
+      }
+      if (hasInt8) {
+        quantization.int8 = {
+          sizeMb: sizeMb('-int8.zip'),
+          url: `${base}-int8.zip`,
+          ...(weights.some((f) => f.endsWith('-int8-apple.zip'))
+            ? { pro: { apple: `${base}-int8-apple.zip` } }
+            : {}),
+        };
+      }
+
       registry[key] = {
         slug: key,
         capabilities: (tags as string[]).filter((t) => !t.includes(':')),
-        quantization: {
-          int4: {
-            sizeMb: sizeMb('-int4.zip'),
-            url: `${base}-int4.zip`,
-            ...(weights.some((f) => f.endsWith('-int4-apple.zip'))
-              ? { pro: { apple: `${base}-int4-apple.zip` } }
-              : {}),
-          },
-          int8: {
-            sizeMb: sizeMb('-int8.zip'),
-            url: `${base}-int8.zip`,
-            ...(weights.some((f) => f.endsWith('-int8-apple.zip'))
-              ? { pro: { apple: `${base}-int8-apple.zip` } }
-              : {}),
-          },
-        },
+        quantization,
       };
     })
   );
 
+  // Apply any runtime overrides set via setModelUrlOverride.
+  for (const slug of Object.keys(urlOverrides)) {
+    const entry = registry[slug];
+    const override = urlOverrides[slug];
+    if (!entry || !override) continue;
+    if (override.proApple && entry.quantization?.int4?.pro) {
+      entry.quantization.int4.pro.apple = override.proApple;
+    }
+    if (override.url && entry.quantization?.int4) {
+      entry.quantization.int4.url = override.url;
+    }
+  }
+
   return registry;
 }
