{"id": "invert-r1", "label": "invert", "prompt": "invert the colors", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main() {\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n    vec3 color = texture(u_texture, snapped).rgb;\n\n    vec3 invertedColor = 1.0 - color;\n\n    outColor = vec4(clamp(invertedColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 7177, "receivedAt": 1776603365324}
{"id": "invert-r2", "label": "invert", "prompt": "invert the colors", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main() {\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n    vec3 color = texture(u_texture, snapped).rgb;\n\n    vec3 invertedColor = 1.0 - color;\n    outColor = vec4(clamp(invertedColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 7714, "receivedAt": 1776603373039}
{"id": "invert-r3", "label": "invert", "prompt": "invert the colors", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main() {\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n    vec3 color = texture(u_texture, snapped).rgb;\n\n    vec3 invertedColor = 1.0 - color;\n    outColor = vec4(clamp(invertedColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 9669, "receivedAt": 1776603382708}
{"id": "invert-r4", "label": "invert", "prompt": "invert the colors", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main() {\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n    vec3 color = texture(u_texture, snapped).rgb;\n\n    vec3 invertedColor = 1.0 - color;\n    outColor = vec4(clamp(invertedColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 9532, "receivedAt": 1776603392241}
{"id": "invert-r5", "label": "invert", "prompt": "invert the colors", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main() {\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n    vec3 color = texture(u_texture, snapped).rgb;\n\n    vec3 invertedColor = 1.0 - color;\n    outColor = vec4(clamp(invertedColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 9527, "receivedAt": 1776603401769}
{"id": "crt-r1", "label": "crt", "prompt": "add a CRT scanline effect", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main() {\n    vec2 uv = v_uv;\n    float scanline_intensity = mod(gl_FragCoord.y, 10.0) / 10.0;\n    float scanline_mask = smoothstep(0.0, 1.0, scanline_intensity * 2.0 - 1.0);\n    \n    vec4 color = texture(u_texture, v_uv);\n    \n    float final_brightness = (1.0 - scanline_mask * 0.1);\n    \n    outColor = vec4(color.rgb * final_brightness, 1.0);\n}", "error": null, "durationMs": 12036, "receivedAt": 1776603413806}
{"id": "crt-r2", "label": "crt", "prompt": "add a CRT scanline effect", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main() {\n    vec2 uv = v_uv;\n    float scanline_intensity = mod(gl_FragCoord.y, 100.0) / 100.0;\n    float scanline_mask = smoothstep(0.0, 1.0, abs(uv.y - scanline_intensity));\n    \n    vec4 originalColor = texture(u_texture, v_uv);\n    \n    vec3 finalColor = originalColor.rgb;\n    \n    float scanline_strength = 0.05;\n    \n    float scanline_factor = sin(uv.x * 10.0 + u_time * 1.5) * 0.05;\n    \n    float scanline_effect = abs(sin(uv.y * 10.0 + u_time * 2.0)) * 0.1;\n\n    float finalScanline = (abs(sin(uv.x * 10.0 + u_time * 1.5)) + finalScanline) * 0.2;\n    \n    finalColor = mix(finalColor, vec3(0.1, 0.1, 0.1), finalScanline);\n    \n    outColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 23421, "receivedAt": 1776603437228}
{"id": "crt-r3", "label": "crt", "prompt": "add a CRT scanline effect", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main() {\n    vec2 uv = v_uv;\n    float scanline_intensity = mod(gl_FragCoord.y, 10.0) / 10.0;\n    float scanline_mask = smoothstep(0.0, 1.0, scanline_intensity);\n\n    vec4 color = texture(u_texture, v_uv);\n\n    float final_brightness = dot(color.rgb, vec3(0.2126, 0.7152, 0.0722));\n    vec3 col;\n    if (final_brightness < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), final_brightness / 0.25);\n    else if (final_brightness < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (final_brightness - 0.25) / 0.25);\n    else if (final_brightness < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (final_brightness - 0.5) / 0.25);\n    else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (final_brightness - 0.75) / 0.25);\n\n    outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\n    float final_scanline = pow(1.0 - scanline_mask, 2.0);\n    outColor.rgb *= final_scanline;\n}", "error": null, "durationMs": 31123, "receivedAt": 1776603468351}
{"id": "crt-r4", "label": "crt", "prompt": "add a CRT scanline effect", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main() {\n    vec2 uv = v_uv;\n    float scanline_intensity = mod(gl_FragCoord.y, 1.0 / 60.0);\n    float scanline_factor = smoothstep(0.0, 0.01, abs(mod(gl_FragCoord.y, 1.0 / 60.0) - 0.5) * 2.0);\n    float scanline = smoothstep(0.0, 1.0, scanline_factor * 2.0 - 1.0);\n    \n    vec4 color = texture(u_texture, v_uv);\n    \n    // Apply a subtle scanline effect by darkening horizontal lines\n    color.rgb *= (1.0 - scanline * 0.15);\n    \n    outColor = color;\n}", "error": null, "durationMs": 14262, "receivedAt": 1776603482614}
{"id": "crt-r5", "label": "crt", "prompt": "add a CRT scanline effect", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "#version 300 es\nprecision mediump float;\nuniform sampler2D u_texture;   // the input photo\nuniform float u_time;          // seconds since start\nuniform vec2 u_resolution;     // pixel dimensions\nin vec2 v_uv;                  // texture coords, 0..1\nout vec4 outColor;\n\nvoid main() {\n    vec3 color = texture(u_texture, v_uv).rgb;\n\n    // CRT Scanline effect: darken alternating horizontal lines\n    // mod(floor(gl_FragCoord.y), 2.0) will be 0.0 for even pixel rows and 1.0 for odd pixel rows.\n    // mix(a, b, t) interpolates between a (t=0) and b (t=1).\n    // So, even lines get a factor of 1.0, and odd lines get a factor of 0.7.\n    float scanlineFactor = mix(1.0, 0.7, mod(floor(gl_FragCoord.y), 2.0));\n\n    color *= scanlineFactor;\n\n    outColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 21028, "receivedAt": 1776603503643}
{"id": "underwater-r1", "label": "underwater", "prompt": "make it look like an underwater scene with caustics", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main()\n{\n    vec2 uv = v_uv;\n    vec2 texelSize = 1.0 / u_resolution;\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(uv / cell) + 0.5) * cell;\n\n    vec3 baseColor = texture(u_texture, snapped).rgb;\n\n    float depth = 1.0 - dot(uv, vec2(0.5, 0.5));\n    float waterEffect = smoothstep(0.5, 0.9, depth);\n\n    vec3 caustics = sin(uv.x * 10.0 + u_time * 2.0) * 0.5 + sin(uv.y * 10.0 + u_time * 3.0) * 0.5;\n    \n    float intensity = pow(caustics, 2.0) * 2.0;\n\n    vec3 finalColor = baseColor + caustics * intensity;\n    \n    outColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 17423, "receivedAt": 1776603521066}
{"id": "underwater-r2", "label": "underwater", "prompt": "make it look like an underwater scene with caustics", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main()\n{\n    vec2 uv = v_uv;\n    float timeFactor = mod(u_time * 1.5, 1.0);\n    \n    vec2 cell = vec2(10.0) / u_resolution;\n    vec2 snapped = (floor(uv / cell) + 0.5) * cell;\n    \n    vec3 baseColor = texture(u_texture, snapped).rgb;\n    \n    float intensity = 0.5 + 0.5 * sin(uv.y * 10.0 + u_time * 3.0);\n    \n    vec3 caustics = vec3(1.0, 0.8, 0.0) * (sin(uv.x * 5.0 + u_time * 1.0) * 0.5 + 0.5);\n    \n    vec3 finalColor = baseColor + caustics * 0.5;\n    \n    outColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 17659, "receivedAt": 1776603538726}
{"id": "underwater-r3", "label": "underwater", "prompt": "make it look like an underwater scene with caustics", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main()\n{\n    vec2 uv = v_uv;\n    float time = u_time * 0.5;\n\n    // Underwater color shift (deeper blue)\n    float blueShift = 0.2 + 0.8 * sin(uv.y * 3.14159 + time);\n\n    // Caustics effect using a grid pattern\n    vec2 cell = vec2(12.0) / u_resolution;\n    vec2 snapped = (floor(uv / cell) + 0.5) * cell;\n    \n    vec3 caustics = texture(u_texture, snapped).rgb;\n    \n    // Combine color with depth/blue shift\n    vec3 baseColor = texture(u_texture, uv).rgb;\n    \n    // Mix the base color with a blue tint and add the caustics\n    vec3 finalColor = mix(baseColor, vec3(0.1, 0.2, 0.4), 0.5) * (1.0 - blueShift) + caustics * 0.5;\n\n    outColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 19016, "receivedAt": 1776603557743}
{"id": "underwater-r4", "label": "underwater", "prompt": "make it look like an underwater scene with caustics", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main()\n{\n    vec2 uv = v_uv;\n    vec2 waterEffect = mod(uv * 10.0 + u_time * 0.5, 1.0);\n    float waterDepth = 1.0 - (uv.y * 0.5 + 0.5); // Deeper towards the bottom of the screen\n\n    vec3 baseColor = texture(u_texture, uv).rgb;\n\n    // Simulate caustics using a noise function based on time and position\n    float noise1 = sin(uv.x * 10.0 + u_time * 2.0) * 0.5 + 0.5;\n    float noise2 = cos(uv.y * 15.0 + u_time * 1.5) * 0.5 + 0.5;\n    float causticStrength = sin(uv.x * 20.0 + u_time * 4.0) * 0.5 + 0.5;\n\n    // Caustics are bright highlights that shift\n    float caustics = (noise1 * 0.5 + 0.5) * causticStrength * 1.5;\n\n    // Apply color shift for blue tint in the water\n    vec3 waterTint = mix(vec3(0.0, 0.1, 0.3), vec3(0.0, 0.3, 0.7), waterDepth);\n\n    // Combine base color with a slight blue shift and add caustics\n    vec3 finalColor = baseColor * waterTint;\n    finalColor += caustics * 0.5;\n\n    outColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 25907, "receivedAt": 1776603583651}
{"id": "underwater-r5", "label": "underwater", "prompt": "make it look like an underwater scene with caustics", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main()\n{\n    vec2 uv = v_uv;\n    float time = u_time * 0.5;\n    \n    vec2 cell = vec2(10.0) / u_resolution;\n    vec2 snapped = (floor(uv / cell) + 0.5) * cell;\n    \n    vec3 baseColor = texture(u_texture, snapped).rgb;\n    \n    float intensity = 1.0 - dot(baseColor, vec3(0.2, 0.2, 0.2));\n    \n    vec3 caustics = pow(1.0 - dot(uv, vec2(1.0, 0.5)), 10.0) * 0.5;\n    \n    vec3 finalColor = baseColor * (1.0 - caustics) + vec3(0.1, 0.3, 0.5) * caustics;\n    \n    outColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 17254, "receivedAt": 1776603600906}
{"id": "vignette-r1", "label": "vignette", "prompt": "apply a vignette that darkens the edges", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main() {\n    vec2 uv = v_uv;\n    float vignette = 1.0 - smoothstep(0.5, 1.0, length(uv - 0.5) * 2.0);\n    outColor = vec4(texture(u_texture, v_uv).rgb * vignette, 1.0);\n}", "error": null, "durationMs": 8109, "receivedAt": 1776603609015}
{"id": "vignette-r2", "label": "vignette", "prompt": "apply a vignette that darkens the edges", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main() {\n    vec2 uv = v_uv;\n    float vignette = 1.0 - smoothstep(0.5, 1.0, length(uv - 0.5) * 2.0);\n    float vignetteStrength = 0.7;\n    float edgeFactor = 1.0 - length(v_uv - 0.5) * 2.0;\n    float vignetteFactor = clamp(edgeFactor, 0.0, 1.0);\n    float vignette = 1.0 - vignetteFactor;\n    \n    vec3 color = texture(u_texture, v_uv).rgb;\n    \n    outColor = vec4(color * vignette, 1.0);\n}", "error": null, "durationMs": 13118, "receivedAt": 1776603622134}
{"id": "vignette-r3", "label": "vignette", "prompt": "apply a vignette that darkens the edges", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main()\n{\n    vec2 uv = v_uv;\n    vec2 center = 0.5;\n    float dist = length(uv - center);\n    float vignette = 1.0 - smoothstep(0.5, 1.5, dist);\n    \n    vec3 color = texture(u_texture, v_uv).rgb;\n    \n    color *= vignette;\n    \n    outColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 10209, "receivedAt": 1776603632344}
{"id": "vignette-r4", "label": "vignette", "prompt": "apply a vignette that darkens the edges", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main() {\n    vec2 uv = v_uv;\n    float vignette = 1.0 - smoothstep(0.5, 1.0, length(uv - 0.5) * 2.0);\n    outColor = vec4(texture(u_texture, v_uv).rgb * vignette, 1.0);\n}", "error": null, "durationMs": 8291, "receivedAt": 1776603640635}
{"id": "vignette-r5", "label": "vignette", "prompt": "apply a vignette that darkens the edges", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main() {\n    vec2 uv = v_uv;\n    float vignette = 1.0 - smoothstep(0.5, 1.0, length(uv - 0.5) * 2.0);\n    \n    vec3 color = texture(u_texture, v_uv).rgb;\n    \n    color *= vignette;\n    \n    outColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 9815, "receivedAt": 1776603650451}
{"id": "neon-r1", "label": "neon", "prompt": "turn it into a neon glow", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main()\n{\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n    vec3 color = texture(u_texture, snapped).rgb;\n\n    float brightness = dot(color, vec3(1.0, 0.1, 0.1));\n    float glowIntensity = pow(brightness, 2.0);\n    vec3 neonColor = mix(vec3(0.0, 0.5, 1.0), vec3(1.0, 0.8, 0.0), glowIntensity);\n    \n    outColor = vec4(clamp(neonColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 14010, "receivedAt": 1776603664463}
{"id": "neon-r2", "label": "neon", "prompt": "turn it into a neon glow", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main()\n{\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n    vec3 color = texture(u_texture, snapped).rgb;\n\n    float luminance = dot(color, vec3(0.2126, 0.7152, 0.0722));\n\n    vec3 finalColor;\n    if (luminance < 0.25)\n        finalColor = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 1.0), luminance / 0.25);\n    else if (luminance < 0.5)\n        finalColor = mix(vec3(0.0, 0.0, 1.0), vec3(0.1, 0.1, 0.1), (luminance - 0.25) / 0.25);\n    else if (luminance < 0.75)\n        finalColor = mix(vec3(0.1, 0.1, 0.1), vec3(0.3, 0.3, 0.1), (luminance - 0.5) / 0.25);\n    else\n        finalColor = mix(vec3(0.3, 0.3, 0.1), vec3(1.0, 1.0, 0.5), (luminance - 0.75) / 0.25);\n\n    outColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 25909, "receivedAt": 1776603690373}
{"id": "neon-r3", "label": "neon", "prompt": "turn it into a neon glow", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main()\n{\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n    vec3 color = texture(u_texture, snapped).rgb;\n\n    float brightness = dot(color, vec3(1.0, 0.0, 0.0)); // Use Red channel for base\n\n    float glowIntensity = pow(brightness, 2.0); // Increase intensity for brighter areas\n\n    vec3 neonColor = mix(vec3(0.2, 0.0, 0.2), vec3(0.5, 0.1, 0.5), glowIntensity * 2.0); // Mix with a blue/cyan glow\n\n    outColor = vec4(neonColor, 1.0);\n}", "error": null, "durationMs": 14199, "receivedAt": 1776603704572}
{"id": "neon-r4", "label": "neon", "prompt": "turn it into a neon glow", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main()\n{\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n\n    vec3 color = texture(u_texture, snapped).rgb;\n\n    float glowIntensity = 0.5;\n    float intensity = pow(1.0 - dot(color, vec3(0.1, 0.1, 0.1)), 2.0); // Simple way to detect dark areas\n\n    vec3 neonColor = mix(vec3(0.1, 0.1, 0.1), vec3(0.1, 0.5, 1.0), intensity);\n\n    outColor = vec4(clamp(color + neonColor * 0.5, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 14882, "receivedAt": 1776603719455}
{"id": "neon-r5", "label": "neon", "prompt": "turn it into a neon glow", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main()\n{\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n\n    vec3 baseColor = texture(u_texture, snapped).rgb;\n\n    float glowIntensity = 0.5;\n    float finalBrightness = 0.1;\n    float intensityFactor = 2.0;\n\n    vec3 finalColor = baseColor * intensityFactor;\n\n    outColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 458049, "receivedAt": 1776604177505}
{"id": "sepia-r1", "label": "sepia", "prompt": "make it look like a sepia-toned vintage photo", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main() {\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n    vec3 color = texture(u_texture, snapped).rgb;\n\n    float sepia_intensity = 0.5;\n    vec3 sepia_tone = vec3(0.9, 0.7, 0.4); // Warm, faded tones\n\n    float t = dot(color, vec3(0.2126, 0.7152, 0.0722));\n    vec3 col;\n\n    if (t < 0.25)      col = mix(vec3(0.1, 0.1, 0.0), sepia_tone, t / 0.25);\n    else if (t < 0.5)  col = mix(sepia_tone, vec3(0.3, 0.1, 0.1), (t - 0.25) / 0.25);\n    else if (t < 0.75) col = mix(vec3(0.3, 0.1, 0.1), vec3(0.5, 0.0, 0.1), (t - 0.5) / 0.25);\n    else               col = mix(vec3(0.5, 0.0, 0.1), vec3(1.0, 0.5, 0.1), (t - 0.75) / 0.25);\n\n    outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 22961, "receivedAt": 1776604200467}
{"id": "sepia-r2", "label": "sepia", "prompt": "make it look like a sepia-toned vintage photo", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main() {\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n    vec3 color = texture(u_texture, snapped).rgb;\n\n    float sepia_factor = 0.8 + 0.2 * sin(u_time * 1.5);\n    vec3 sepia_tone = mix(vec3(0.9, 0.7, 0.4), vec3(0.8, 0.6, 0.2), sepia_factor);\n\n    vec3 final_color = mix(color, sepia_tone, 0.5);\n\n    outColor = vec4(clamp(final_color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 13391, "receivedAt": 1776604213859}
{"id": "sepia-r3", "label": "sepia", "prompt": "make it look like a sepia-toned vintage photo", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main() {\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n    vec3 color = texture(u_texture, snapped).rgb;\n\n    float sepia_intensity = 0.5 + 0.5 * sin(v_uv.y * 10.0 + u_time);\n    vec3 sepia_tone = mix(vec3(0.9, 0.7, 0.5), vec3(0.8, 0.5, 0.2), sepia_intensity);\n\n    vec3 final_color = mix(color, sepia_tone, 0.7);\n\n    outColor = vec4(clamp(final_color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 13371, "receivedAt": 1776604227231}
{"id": "sepia-r4", "label": "sepia", "prompt": "make it look like a sepia-toned vintage photo", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main() {\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n    vec3 color = texture(u_texture, snapped).rgb;\n\n    float sepia_intensity = 0.5 + 0.5 * sin(v_uv.y * 10.0 + u_time);\n    vec3 sepia_tone = mix(vec3(0.9, 0.7, 0.4), vec3(1.0, 0.8, 0.6), sepia_intensity);\n\n    vec3 final_color = mix(color, sepia_tone, 0.5);\n\n    outColor = vec4(clamp(final_color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 13384, "receivedAt": 1776604240615}
{"id": "sepia-r5", "label": "sepia", "prompt": "make it look like a sepia-toned vintage photo", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main() {\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n    vec3 color = texture(u_texture, snapped).rgb;\n\n    float sepia_factor = 0.7;\n    vec3 sepia_tone = vec3(0.9, 0.7, 0.4); // Warm, faded tones\n\n    float intensity = 1.0 - dot(color, vec3(0.2126, 0.7152, 0.0722));\n\n    vec3 final_color = mix(color, sepia_tone, intensity * 0.5);\n    final_color = mix(final_color, vec3(0.8, 0.6, 0.4), 1.0 - intensity * 0.5);\n\n    outColor = vec4(clamp(final_color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 14969, "receivedAt": 1776604255584}
{"id": "posterize-r1", "label": "posterize", "prompt": "posterize the image to 4 color bands per channel", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main()\n{\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n\n    float r = texture(u_texture, snapped).r;\n    float g = texture(u_texture, snapped).g;\n    float b = texture(u_texture, snapped).b;\n\n    float r_band = floor(r * 4.0) / 4.0;\n    float g_band = floor(g * 4.0) / 4.0;\n    float b_band = floor(b * 4.0) / 4.0;\n\n    vec3 finalColor = vec3(r_band, g_band, b_band);\n    outColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 14319, "receivedAt": 1776604269904}
{"id": "posterize-r2", "label": "posterize", "prompt": "posterize the image to 4 color bands per channel", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main()\n{\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n    vec3 color = texture(u_texture, snapped).rgb;\n\n    float r = color.r;\n    float g = color.g;\n    float b = color.b;\n\n    float r_band = floor(r * 4.0) / 4.0;\n    float g_band = floor(g * 4.0) / 4.0;\n    float b_band = floor(b * 4.0) / 4.0;\n\n    vec3 final_color = vec3(r_band, g_band, b_band);\n\n    outColor = vec4(clamp(final_color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 13815, "receivedAt": 1776604283719}
{"id": "posterize-r3", "label": "posterize", "prompt": "posterize the image to 4 color bands per channel", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main() {\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n\n    float r = texture(u_texture, snapped).r;\n    float g = texture(u_texture, snapped).g;\n    float b = texture(u_texture, snapped).b;\n\n    vec3 color = vec3(r, g, b);\n\n    float r_p = floor(color.r * 4.0) / 4.0;\n    float g_p = floor(color.g * 4.0) / 4.0;\n    float b_p = floor(color.b * 4.0) / 4.0;\n\n    vec3 finalColor = vec3(r_p, g_p, b_p);\n\n    outColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 14113, "receivedAt": 1776604297832}
{"id": "posterize-r4", "label": "posterize", "prompt": "posterize the image to 4 color bands per channel", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main()\n{\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n\n    float r = texture(u_texture, snapped).r;\n    float g = texture(u_texture, snapped).g;\n    float b = texture(u_texture, snapped).b;\n\n    float r_band = floor(r * 4.0) / 4.0;\n    float g_band = floor(g * 4.0) / 4.0;\n    float b_band = floor(b * 4.0) / 4.0;\n\n    vec3 finalColor = vec3(r_band, g_band, b_band);\n\n    outColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 13224, "receivedAt": 1776604311058}
{"id": "posterize-r5", "label": "posterize", "prompt": "posterize the image to 4 color bands per channel", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main() {\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n\n    float r = texture(u_texture, snapped).r;\n    float g = texture(u_texture, snapped).g;\n    float b = texture(u_texture, snapped).b;\n\n    vec3 color = vec3(r, g, b);\n\n    float r_p = mod(color.r, 4.0) / 4.0;\n    float g_p = mod(color.g, 4.0) / 4.0;\n    float b_p = mod(color.b, 4.0) / 4.0;\n\n    vec3 finalColor = vec3(r_p, g_p, b_p);\n\n    outColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 14032, "receivedAt": 1776604325090}
{"id": "pixelate-r1", "label": "pixelate", "prompt": "pixelate the image with chunky 16x16 pixel blocks", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main()\n{\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n    vec3 color = texture(u_texture, snapped).rgb;\n    outColor = vec4(color, 1.0);\n}", "error": null, "durationMs": 7118, "receivedAt": 1776604332208}
{"id": "pixelate-r2", "label": "pixelate", "prompt": "pixelate the image with chunky 16x16 pixel blocks", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main()\n{\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n    vec3 color = texture(u_texture, snapped).rgb;\n    outColor = vec4(color, 1.0);\n}", "error": null, "durationMs": 7178, "receivedAt": 1776604339388}
{"id": "pixelate-r3", "label": "pixelate", "prompt": "pixelate the image with chunky 16x16 pixel blocks", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main()\n{\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n    vec3 color = texture(u_texture, snapped).rgb;\n    outColor = vec4(color, 1.0);\n}", "error": null, "durationMs": 7140, "receivedAt": 1776604346528}
{"id": "pixelate-r4", "label": "pixelate", "prompt": "pixelate the image with chunky 16x16 pixel blocks", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main()\n{\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n    vec3 color = texture(u_texture, snapped).rgb;\n    outColor = vec4(color, 1.0);\n}", "error": null, "durationMs": 1063355, "receivedAt": 1776605409884}
{"id": "pixelate-r5", "label": "pixelate", "prompt": "pixelate the image with chunky 16x16 pixel blocks", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main()\n{\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n    vec3 color = texture(u_texture, snapped).rgb;\n    outColor = vec4(color, 1.0);\n}", "error": null, "durationMs": 7541, "receivedAt": 1776605417425}
{"id": "thermal-r1", "label": "thermal", "prompt": "render it as a thermal camera image with cool blacks and hot whites going through blue, purple, red, yellow", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main() {\n    vec2 uv = v_uv;\n    float t = dot(texture(u_texture, uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n    vec3 col;\n    if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n    else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n    else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n    else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n    outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 20046, "receivedAt": 1776605437472}
{"id": "thermal-r2", "label": "thermal", "prompt": "render it as a thermal camera image with cool blacks and hot whites going through blue, purple, red, yellow", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main() {\n    vec3 color = texture(u_texture, v_uv).rgb;\n    float t = dot(color, vec3(0.2126, 0.7152, 0.0722));\n    vec3 col;\n    if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n    else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n    else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n    else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n    outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 19826, "receivedAt": 1776605457299}
{"id": "thermal-r3", "label": "thermal", "prompt": "render it as a thermal camera image with cool blacks and hot whites going through blue, purple, red, yellow", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main() {\n    vec3 color = texture(u_texture, v_uv).rgb;\n    float t = dot(color, vec3(0.2126, 0.7152, 0.0722));\n    vec3 col;\n    if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n    else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n    else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n    else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n    outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 20087, "receivedAt": 1776605477387}
{"id": "thermal-r4", "label": "thermal", "prompt": "render it as a thermal camera image with cool blacks and hot whites going through blue, purple, red, yellow", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main() {\n    vec2 uv = v_uv;\n    float t = dot(texture(u_texture, uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n    vec3 col;\n    if (t < 0.25)\n        col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n    else if (t < 0.5)\n        col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n    else if (t < 0.75)\n        col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n    else\n        col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n    outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 823707, "receivedAt": 1776606301095}
{"id": "thermal-r5", "label": "thermal", "prompt": "render it as a thermal camera image with cool blacks and hot whites going through blue, purple, red, yellow", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main() {\n    vec3 color = texture(u_texture, v_uv).rgb;\n    float t = dot(color, vec3(0.2126, 0.7152, 0.0722));\n    vec3 col;\n    if (t < 0.25)\n        col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n    else if (t < 0.5)\n        col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n    else if (t < 0.75)\n        col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n    else\n        col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n    outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 17842, "receivedAt": 1776606318938}
{"id": "glitch-r1", "label": "glitch", "prompt": "add a chromatic-aberration glitch effect: sample red and blue channels with a slight horizontal offset", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;   // the input photo\nuniform float u_time;          // seconds since start\nuniform vec2 u_resolution;     // pixel dimensions\nin vec2 v_uv;                  // texture coords, 0..1\nout vec4 outColor;\n\nvoid main() {\n  vec2 offset = vec2(0.005, 0.0); // Small horizontal offset\n\n  float r = texture(u_texture, v_uv - offset).r;\n  float g = texture(u_texture, v_uv).g;\n  float b = texture(u_texture, v_uv + offset).b;\n\n  outColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 10919, "receivedAt": 1776606329858}
{"id": "glitch-r2", "label": "glitch", "prompt": "add a chromatic-aberration glitch effect: sample red and blue channels with a slight horizontal offset", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main()\n{\n    vec2 uv = v_uv;\n    float offset = 0.01 * sin(u_time * 5.0 + uv.y * 10.0);\n    \n    vec2 uvRed = uv + vec2(offset, 0.0);\n    vec2 uvBlue = uv - vec2(offset, 0.0);\n\n    vec3 red = texture(u_texture, uvRed).r;\n    vec3 blue = texture(u_texture, uvBlue).b;\n\n    vec3 finalColor = red + blue;\n    \n    outColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 9432, "receivedAt": 1776606339291}
{"id": "glitch-r3", "label": "glitch", "prompt": "add a chromatic-aberration glitch effect: sample red and blue channels with a slight horizontal offset", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main()\n{\n    vec2 uv = v_uv;\n    float offset = 0.01 * sin(u_time * 5.0);\n    vec2 offsetUV = uv + vec2(offset, 0.0);\n\n    float r = texture(u_texture, offsetUV).r;\n    float b = texture(u_texture, offsetUV + vec2(0.0, 0.01)).b;\n\n    float glitchFactor = 0.05;\n    float glitchStrength = 0.02;\n\n    float finalR = r + glitchFactor * (sin(u_time * 10.0) * glitchStrength);\n    float finalB = b + glitchFactor * (cos(u_time * 12.0) * glitchStrength);\n\n    vec3 finalColor = vec3(finalR, 0.0, finalB);\n\n    outColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 12442, "receivedAt": 1776606351734}
{"id": "glitch-r4", "label": "glitch", "prompt": "add a chromatic-aberration glitch effect: sample red and blue channels with a slight horizontal offset", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;   // the input photo\nuniform float u_time;          // seconds since start\nuniform vec2 u_resolution;     // pixel dimensions\nin vec2 v_uv;                  // texture coords, 0..1\nout vec4 outColor;\n\nvoid main() {\n  // Define a small horizontal offset, relative to resolution\n  // This value can be tweaked for stronger/weaker aberration\n  float offset = 2.0 / u_resolution.x; // 2 pixels offset\n\n  // Sample red channel with an offset to the left\n  vec4 redTex = texture(u_texture, v_uv - vec2(offset, 0.0));\n  // Sample green channel at the original UV\n  vec4 greenTex = texture(u_texture, v_uv);\n  // Sample blue channel with an offset to the right\n  vec4 blueTex = texture(u_texture, v_uv + vec2(offset, 0.0));\n\n  // Combine the channels\n  vec3 finalColor = vec3(redTex.r, greenTex.g, blueTex.b);\n\n  // Clamp the final RGB and output\n  outColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 14588, "receivedAt": 1776606366323}
{"id": "glitch-r5", "label": "glitch", "prompt": "add a chromatic-aberration glitch effect: sample red and blue channels with a slight horizontal offset", "systemPrompt": "You generate GLSL ES 3.00 fragment shaders for WebGL 2. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the in/out at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  in vec2 v_uv;                  // texture coords, 0..1\n  out vec4 outColor;\n\nWrite the final color to outColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white. Use texture() (not texture2D()) to sample.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  outColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nin vec2 v_uv;\nout vec4 outColor;\n\nvoid main()\n{\n    vec2 uv = v_uv;\n    float offset = 0.01 * sin(u_time * 5.0);\n    vec2 offsetUV = uv + vec2(offset, 0.0);\n\n    float r = texture(u_texture, offsetUV).r;\n    float b = texture(u_texture, offsetUV + vec2(0.0, 0.01)).b;\n\n    float glitch = (r - b) * 0.5;\n\n    vec3 color = vec3(r, 0.0, b);\n\n    outColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 9587, "receivedAt": 1776606375910}
