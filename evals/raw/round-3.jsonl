{"id": "invert-r1", "label": "invert", "prompt": "invert the colors", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n  vec3 originalColor = texture2D(u_texture, v_uv).rgb;\n  vec3 invertedColor = 1.0 - originalColor;\n  gl_FragColor = vec4(clamp(invertedColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 8865, "receivedAt": 1776608982108}
{"id": "invert-r2", "label": "invert", "prompt": "invert the colors", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n  vec3 originalColor = texture2D(u_texture, v_uv).rgb;\n  vec3 invertedColor = 1.0 - originalColor;\n  gl_FragColor = vec4(clamp(invertedColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 8883, "receivedAt": 1776608990991}
{"id": "invert-r3", "label": "invert", "prompt": "invert the colors", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n  vec3 originalColor = texture2D(u_texture, v_uv).rgb;\n  vec3 invertedColor = 1.0 - originalColor;\n  gl_FragColor = vec4(clamp(invertedColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 8828, "receivedAt": 1776608999820}
{"id": "invert-r4", "label": "invert", "prompt": "invert the colors", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n  vec3 originalColor = texture2D(u_texture, v_uv).rgb;\n  vec3 invertedColor = 1.0 - originalColor;\n  gl_FragColor = vec4(clamp(invertedColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 8806, "receivedAt": 1776609008626}
{"id": "invert-r5", "label": "invert", "prompt": "invert the colors", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n  vec3 originalColor = texture2D(u_texture, v_uv).rgb;\n  vec3 invertedColor = 1.0 - originalColor;\n  gl_FragColor = vec4(clamp(invertedColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 8808, "receivedAt": 1776609017436}
{"id": "crt-r1", "label": "crt", "prompt": "add a CRT scanline effect", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec2 scanline = floor(v_uv.y * 60.0) * 1.0 / u_resolution.y;\n    float scanlineStrength = 0.05;\n    float scanlineIntensity = sin(v_uv.x * 10.0 + u_time * 1.0) * 0.05;\n    float finalScanline = scanline + scanlineIntensity;\n    \n    vec3 color = texture2D(u_texture, v_uv).rgb;\n    \n    color *= (1.0 - finalScanline);\n    \n    gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 14451, "receivedAt": 1776609031888}
{"id": "crt-r2", "label": "crt", "prompt": "add a CRT scanline effect", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec2 scanline = floor(v_uv.y * 60.0) * 1.0 / u_resolution.y;\n    float scanlineStrength = 0.02;\n    float scanlineIntensity = 1.0 - abs(v_uv.y - scanline);\n    float scanlineFactor = smoothstep(0.0, 1.0, scanlineIntensity);\n    \n    vec3 color = texture2D(u_texture, v_uv).rgb;\n    \n    color *= mix(color, vec3(0.1, 0.1, 0.1), scanlineFactor);\n    \n    gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 14928, "receivedAt": 1776609046817}
{"id": "crt-r3", "label": "crt", "prompt": "add a CRT scanline effect", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec2 scanline = floor(v_uv.y * 60.0) * 1.0 / u_resolution.y;\n    float scanlineIntensity = mod(v_uv.y * 60.0, 1.0);\n    float scanlineFactor = 1.0 - abs(v_uv.y - scanlineIntensity);\n    float scanlineMask = smoothstep(0.0, 1.0, abs(v_uv.y - scanlineIntensity) * 10.0);\n    \n    vec3 color = texture2D(u_texture, v_uv).rgb;\n    \n    color *= (1.0 - scanlineMask * 0.15);\n    \n    gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 16060, "receivedAt": 1776609062878}
{"id": "crt-r4", "label": "crt", "prompt": "add a CRT scanline effect", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec2 scanline = floor(v_uv.y * 60.0) / 60.0;\n    float scanlineIntensity = smoothstep(scanline - 0.5, scanline + 0.5, v_uv.y);\n    \n    vec3 originalColor = texture2D(u_texture, v_uv).rgb;\n    \n    // Subtle darkening/brightening for scanline effect\n    float scanLineFactor = 1.0 - abs(mod(v_uv.y * 60.0, 1.0) - 0.5);\n    \n    vec3 finalColor = originalColor * (0.95 + scanLineFactor * 0.05);\n    \n    gl_FragColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 16005, "receivedAt": 1776609078887}
{"id": "crt-r5", "label": "crt", "prompt": "add a CRT scanline effect", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec2 scanline = floor(v_uv.y * 60.0) * 1.0;\n    float scanlineStrength = 0.5;\n    float scanlineIntensity = sin(v_uv.x * 100.0 + u_time * 5.0) * 0.5 + 0.5;\n    \n    vec3 finalColor = texture2D(u_texture, v_uv).rgb;\n    \n    finalColor.r *= (1.0 - scanlineStrength * scanlineIntensity);\n    finalColor.g *= (1.0 - scanlineStrength * scanlineIntensity);\n    finalColor.b *= (1.0 - scanlineStrength * scanlineIntensity);\n    \n    gl_FragColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 16137, "receivedAt": 1776609095024}
{"id": "underwater-r1", "label": "underwater", "prompt": "make it look like an underwater scene with caustics", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n    vec3 color = texture2D(u_texture, uv).rgb;\n    float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n    color *= mix(0.7, 1.3, caustic);\n    color *= vec3(0.6, 0.9, 1.1);\n    gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 17073, "receivedAt": 1776609112098}
{"id": "underwater-r2", "label": "underwater", "prompt": "make it look like an underwater scene with caustics", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n    vec3 color = texture2D(u_texture, uv).rgb;\n    float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n    color *= mix(0.7, 1.3, caustic);\n    color *= vec3(0.6, 0.9, 1.1);\n    gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 16874, "receivedAt": 1776609128973}
{"id": "underwater-r3", "label": "underwater", "prompt": "make it look like an underwater scene with caustics", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n    vec3 color = texture2D(u_texture, uv).rgb;\n    float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n    color *= mix(0.7, 1.3, caustic);\n    color *= vec3(0.6, 0.9, 1.1);\n    gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 17281, "receivedAt": 1776609146254}
{"id": "underwater-r4", "label": "underwater", "prompt": "make it look like an underwater scene with caustics", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 17039, "receivedAt": 1776609163294}
{"id": "underwater-r5", "label": "underwater", "prompt": "make it look like an underwater scene with caustics", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n    vec3 color = texture2D(u_texture, uv).rgb;\n    float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n    color *= mix(0.7, 1.3, caustic);\n    color *= vec3(0.6, 0.9, 1.1);\n    gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 16958, "receivedAt": 1776609180252}
{"id": "vignette-r1", "label": "vignette", "prompt": "apply a vignette that darkens the edges", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec2 uv = v_uv * 2.0;\n    float dist = length(uv - 0.5) * 1.5;\n    float vignette = 1.0 - smoothstep(0.5, 1.5, dist);\n    vec3 color = texture2D(u_texture, v_uv).rgb;\n    color *= vignette;\n    gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 11283, "receivedAt": 1776609191536}
{"id": "vignette-r2", "label": "vignette", "prompt": "apply a vignette that darkens the edges", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec2 center = 0.5;\n    vec2 uv = v_uv * 2.0 - 1.0;\n    float dist = length(v_uv - center);\n    float vignette = 1.0 - smoothstep(0.5, 1.0, dist);\n    \n    vec3 color = texture2D(u_texture, v_uv).rgb;\n    \n    color *= vignette;\n    \n    gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 12368, "receivedAt": 1776609203905}
{"id": "vignette-r3", "label": "vignette", "prompt": "apply a vignette that darkens the edges", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec2 uv = v_uv * 2.0 - 1.0;\n    float dist = length(uv);\n    float vignette = 1.0 - smoothstep(0.5, 1.0, dist);\n    vec3 color = texture2D(u_texture, v_uv).rgb;\n    color *= mix(1.0, 0.5, vignette);\n    gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 11739, "receivedAt": 1776609215644}
{"id": "vignette-r4", "label": "vignette", "prompt": "apply a vignette that darkens the edges", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec2 uv = v_uv * 2.0 - 1.0;\n    float dist = length(uv);\n    float vignette = 1.0 - smoothstep(0.5, 1.0, dist);\n    vec3 color = texture2D(u_texture, v_uv).rgb;\n    color *= mix(1.0, 0.5, vignette);\n    gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 11661, "receivedAt": 1776609227306}
{"id": "vignette-r5", "label": "vignette", "prompt": "apply a vignette that darkens the edges", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec2 uv = v_uv * 2.0;\n    float dist = length(uv - 0.5) * 1.5;\n    float vignette = smoothstep(0.5, 1.0, dist);\n    vec3 color = texture2D(u_texture, v_uv).rgb;\n    color *= mix(1.0, 0.7, vignette);\n    gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 11817, "receivedAt": 1776609239124}
{"id": "neon-r1", "label": "neon", "prompt": "turn it into a neon glow", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec3 color = texture2D(u_texture, v_uv).rgb;\n    float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n    vec3 sat = mix(vec3(lum), color, 1.8);\n    vec3 glow = sat * lum * 1.5;\n    gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 12111, "receivedAt": 1776609251235}
{"id": "neon-r2", "label": "neon", "prompt": "turn it into a neon glow", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 12149, "receivedAt": 1776609263384}
{"id": "neon-r3", "label": "neon", "prompt": "turn it into a neon glow", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 23341, "receivedAt": 1776609286726}
{"id": "neon-r4", "label": "neon", "prompt": "turn it into a neon glow", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 21092, "receivedAt": 1776609307819}
{"id": "neon-r5", "label": "neon", "prompt": "turn it into a neon glow", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec3 color = texture2D(u_texture, v_uv).rgb;\n    float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n    vec3 sat = mix(vec3(lum), color, 1.8);\n    vec3 glow = sat * lum * 1.5;\n    gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 12332, "receivedAt": 1776609320151}
{"id": "sepia-r1", "label": "sepia", "prompt": "make it look like a sepia-toned vintage photo", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec3 originalColor = texture2D(u_texture, v_uv).rgb;\n    float sepiaIntensity = 0.15;\n    vec3 sepiaTone = vec3(0.8, 0.6, 0.2);\n    vec3 finalColor = mix(originalColor, sepiaTone, sepiaIntensity);\n    gl_FragColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 11069, "receivedAt": 1776609331221}
{"id": "sepia-r2", "label": "sepia", "prompt": "make it look like a sepia-toned vintage photo", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec3 color = texture2D(u_texture, v_uv).rgb;\n    float sepia_intensity = dot(color, vec3(0.9, 0.7, 0.1));\n    vec3 sepia_tone = mix(vec3(0.5, 0.3, 0.1), vec3(0.9, 0.7, 0.1), sepia_intensity);\n    gl_FragColor = vec4(clamp(sepia_tone, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 12858, "receivedAt": 1776609344079}
{"id": "sepia-r3", "label": "sepia", "prompt": "make it look like a sepia-toned vintage photo", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec3 color = texture2D(u_texture, v_uv).rgb;\n    float sepia_intensity = dot(color, vec3(0.393, 0.769, 0.18), vec3(0.2126, 0.7152, 0.0722));\n    vec3 sepia_tint = vec3(0.9, 0.7, 0.3);\n    vec3 final_color = mix(color, sepia_tint, sepia_intensity);\n    gl_FragColor = vec4(clamp(final_color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 14359, "receivedAt": 1776609358438}
{"id": "sepia-r4", "label": "sepia", "prompt": "make it look like a sepia-toned vintage photo", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec3 color = texture2D(u_texture, v_uv).rgb;\n    float sepia_intensity = dot(color, vec3(0.393, 0.769, 0.188));\n    vec3 sepia_tone = mix(vec3(0.8, 0.6, 0.4), vec3(0.9, 0.7, 0.3), sepia_intensity);\n    gl_FragColor = vec4(clamp(sepia_tone, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 13099, "receivedAt": 1776609371538}
{"id": "sepia-r5", "label": "sepia", "prompt": "make it look like a sepia-toned vintage photo", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n\n  // Calculate luminance\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n\n  // Apply sepia tone by adjusting color channels based on luminance\n  vec3 sepiaColor;\n  sepiaColor.r = lum * 1.1;\n  sepiaColor.g = lum * 0.9;\n  sepiaColor.b = lum * 0.7;\n  \n  gl_FragColor = vec4(clamp(sepiaColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 28733, "receivedAt": 1776609400272}
{"id": "posterize-r1", "label": "posterize", "prompt": "posterize the image to 4 color bands per channel", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec3 color = texture2D(u_texture, v_uv).rgb;\n    \n    float r_quant = floor(color.r * 4.0) / 4.0;\n    float g_quant = floor(color.g * 4.0) / 4.0;\n    float b_quant = floor(color.b * 4.0) / 4.0;\n    \n    vec3 quantized_color = vec3(r_quant, g_quant, b_quant);\n    \n    gl_FragColor = vec4(clamp(quantized_color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 13844, "receivedAt": 1776609414117}
{"id": "posterize-r2", "label": "posterize", "prompt": "posterize the image to 4 color bands per channel", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec3 color = texture2D(u_texture, v_uv).rgb;\n    float intensity = dot(color, vec3(0.2126, 0.7152, 0.0722));\n\n    vec3 finalColor = vec3(0.0);\n\n    float bandCount = 4.0;\n    float step = 1.0 / bandCount;\n\n    for (int i = 0; i < 3; i++) {\n        float t = intensity * float(i + 1) * step;\n        vec3 band;\n        if (i == 0) {\n            band = vec3(0.0);\n        } else if (i == 1) {\n            band = vec3(0.1, 0.1, 0.1);\n        } else {\n            band = vec3(0.2, 0.2, 0.2);\n        }\n\n        finalColor += band;\n    }\n\n    gl_FragColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 19923, "receivedAt": 1776609434040}
{"id": "posterize-r3", "label": "posterize", "prompt": "posterize the image to 4 color bands per channel", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec3 color = texture2D(u_texture, v_uv).rgb;\n    float threshold = 4.0;\n    float step = 1.0 / threshold;\n    \n    vec3 finalColor;\n    finalColor.r = floor(color.r * threshold) / threshold;\n    finalColor.g = floor(color.g * threshold) / threshold;\n    finalColor.b = floor(color.b * threshold) / threshold;\n    \n    gl_FragColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 12659, "receivedAt": 1776609446700}
{"id": "posterize-r4", "label": "posterize", "prompt": "posterize the image to 4 color bands per channel", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec3 color = texture2D(u_texture, v_uv).rgb;\n    \n    float r_step = 4.0;\n    float g_step = 4.0;\n    float b_step = 4.0;\n    \n    float r_fade = mod(color.r, r_step) / r_step;\n    float g_fade = mod(color.g, g_step) / g_step;\n    float b_fade = mod(color.b, b_step) / b_step;\n    \n    vec3 final_color = mix(vec3(0.0), vec3(1.0), r_fade);\n    final_color = mix(final_color, vec3(0.0), g_fade);\n    final_color = mix(final_color, vec3(0.0), b_fade);\n    \n    gl_FragColor = vec4(clamp(final_color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 18560, "receivedAt": 1776609465260}
{"id": "posterize-r5", "label": "posterize", "prompt": "posterize the image to 4 color bands per channel", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec3 color = texture2D(u_texture, v_uv).rgb;\n    float threshold = 1.0 / 4.0;\n    vec3 posterizedColor = vec3(\n        step(color.r, threshold) * 1.0,\n        step(color.g, threshold) * 1.0,\n        step(color.b, threshold) * 1.0\n    );\n\n    vec3 finalColor = vec3(\n        step(color.r, threshold) * 1.0,\n        step(color.g, threshold) * 1.0,\n        step(color.b, threshold) * 1.0\n    );\n\n    gl_FragColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 15857, "receivedAt": 1776609481118}
{"id": "pixelate-r1", "label": "pixelate", "prompt": "pixelate the image with chunky 16x16 pixel blocks", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n    vec3 color = texture2D(u_texture, snapped).rgb;\n    gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 9892, "receivedAt": 1776609491011}
{"id": "pixelate-r2", "label": "pixelate", "prompt": "pixelate the image with chunky 16x16 pixel blocks", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 10166, "receivedAt": 1776609501178}
{"id": "pixelate-r3", "label": "pixelate", "prompt": "pixelate the image with chunky 16x16 pixel blocks", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec2 cell = vec2(16.0) / u_resolution;\n    vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n    vec3 color = texture2D(u_texture, snapped).rgb;\n    gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 10252, "receivedAt": 1776609511430}
{"id": "pixelate-r4", "label": "pixelate", "prompt": "pixelate the image with chunky 16x16 pixel blocks", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 10302, "receivedAt": 1776609521733}
{"id": "pixelate-r5", "label": "pixelate", "prompt": "pixelate the image with chunky 16x16 pixel blocks", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 10238, "receivedAt": 1776609531971}
{"id": "thermal-r1", "label": "thermal", "prompt": "render it as a thermal camera image with cool blacks and hot whites going through blue, purple, red, yellow", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n  vec2 uv = v_uv;\n  float t = dot(texture2D(u_texture, uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)\n    col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)\n    col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75)\n    col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else\n    col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 25856, "receivedAt": 1776609557828}
{"id": "thermal-r2", "label": "thermal", "prompt": "render it as a thermal camera image with cool blacks and hot whites going through blue, purple, red, yellow", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n  vec2 uv = v_uv;\n  float t = dot(texture2D(u_texture, uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)\n    col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)\n    col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75)\n    col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else\n    col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 25956, "receivedAt": 1776609583785}
{"id": "thermal-r3", "label": "thermal", "prompt": "render it as a thermal camera image with cool blacks and hot whites going through blue, purple, red, yellow", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float t = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 29531, "receivedAt": 1776609613317}
{"id": "thermal-r4", "label": "thermal", "prompt": "render it as a thermal camera image with cool blacks and hot whites going through blue, purple, red, yellow", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    vec2 uv = v_uv;\n    float t = dot(texture2D(u_texture, uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n    vec3 col;\n    if (t < 0.25)\n        col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n    else if (t < 0.5)\n        col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n    else if (t < 0.75)\n        col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n    else\n        col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n    gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 25950, "receivedAt": 1776609639267}
{"id": "thermal-r5", "label": "thermal", "prompt": "render it as a thermal camera image with cool blacks and hot whites going through blue, purple, red, yellow", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n  vec2 uv = v_uv;\n  float t = dot(texture2D(u_texture, uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)\n    col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)\n    col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75)\n    col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else\n    col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 25917, "receivedAt": 1776609665185}
{"id": "glitch-r1", "label": "glitch", "prompt": "add a chromatic-aberration glitch effect: sample red and blue channels with a slight horizontal offset", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    float off = 0.005;\n    float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n    float g = texture2D(u_texture, v_uv).g;\n    float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n    \n    vec3 finalColor = vec3(r, g, b);\n    \n    gl_FragColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 13053, "receivedAt": 1776609678238}
{"id": "glitch-r2", "label": "glitch", "prompt": "add a chromatic-aberration glitch effect: sample red and blue channels with a slight horizontal offset", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n    float off = 0.005;\n    float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n    float g = texture2D(u_texture, v_uv).g;\n    float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n    \n    vec3 finalColor = vec3(r, g, b);\n    \n    gl_FragColor = vec4(clamp(finalColor, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 12893, "receivedAt": 1776609691131}
{"id": "glitch-r3", "label": "glitch", "prompt": "add a chromatic-aberration glitch effect: sample red and blue channels with a slight horizontal offset", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n  float off = 0.005;\n  \n  float r_channel = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g_channel = texture2D(u_texture, v_uv).g;\n  float b_channel = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n\n  vec3 final_color = vec3(r_channel, g_channel, b_channel);\n  \n  gl_FragColor = vec4(clamp(final_color, 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 13712, "receivedAt": 1776609704844}
{"id": "glitch-r4", "label": "glitch", "prompt": "add a chromatic-aberration glitch effect: sample red and blue channels with a slight horizontal offset", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 12068, "receivedAt": 1776609716913}
{"id": "glitch-r5", "label": "glitch", "prompt": "add a chromatic-aberration glitch effect: sample red and blue channels with a slight horizontal offset", "systemPrompt": "You generate GLSL ES 1.00 fragment shaders for WebGL 1. Output ONLY the shader source code \u2014 no explanation, no markdown code fences, no comments outside the shader.\n\nThe shader runs over a photo. Declare these uniforms and the varying at the top of your output:\n\n  precision mediump float;\n  uniform sampler2D u_texture;   // the input photo\n  uniform float u_time;          // seconds since start\n  uniform vec2 u_resolution;     // pixel dimensions\n  varying vec2 v_uv;             // texture coords, 0..1\n\nWrite the final color to gl_FragColor. Modify the photo according to the user's request. Clamp the final RGB to [0, 1] so additive effects don't blow out to white.\n\nReference snippet for grid/block effects (square cells):\n\n  vec2 cell = vec2(16.0) / u_resolution;\n  vec2 snapped = (floor(v_uv / cell) + 0.5) * cell;\n  vec3 color = texture2D(u_texture, snapped).rgb;\n\nReference snippet for luminance-to-color-ramp shaders (e.g. thermal/false-color):\n\n  float t = dot(texture2D(u_texture, v_uv).rgb, vec3(0.2126, 0.7152, 0.0722));\n  vec3 col;\n  if (t < 0.25)      col = mix(vec3(0.0, 0.0, 0.0), vec3(0.0, 0.0, 0.6), t / 0.25);\n  else if (t < 0.5)  col = mix(vec3(0.0, 0.0, 0.6), vec3(0.5, 0.0, 0.6), (t - 0.25) / 0.25);\n  else if (t < 0.75) col = mix(vec3(0.5, 0.0, 0.6), vec3(0.95, 0.1, 0.05), (t - 0.5) / 0.25);\n  else               col = mix(vec3(0.95, 0.1, 0.05), vec3(1.0, 1.0, 0.2), (t - 0.75) / 0.25);\n  gl_FragColor = vec4(clamp(col, 0.0, 1.0), 1.0);\n\nReference snippet for neon glow:\n\n  vec3 color = texture2D(u_texture, v_uv).rgb;\n  float lum = dot(color, vec3(0.2126, 0.7152, 0.0722));\n  vec3 sat = mix(vec3(lum), color, 1.8);\n  vec3 glow = sat * lum * 1.5;\n  gl_FragColor = vec4(clamp(sat + glow * 0.4, 0.0, 1.0), 1.0);\n\nReference snippet for underwater caustics:\n\n  vec2 uv = v_uv + 0.01 * vec2(sin(v_uv.y * 30.0 + u_time * 2.0), cos(v_uv.x * 30.0 + u_time * 2.0));\n  vec3 color = texture2D(u_texture, uv).rgb;\n  float caustic = 0.5 + 0.5 * sin(v_uv.x * 50.0 + u_time * 3.0) * sin(v_uv.y * 50.0 + u_time * 2.0);\n  color *= mix(0.7, 1.3, caustic);\n  color *= vec3(0.6, 0.9, 1.1);\n  gl_FragColor = vec4(clamp(color, 0.0, 1.0), 1.0);\n\nReference snippet for chromatic aberration:\n\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n\nIMPORTANT: The first non-empty line of your output MUST be exactly:\n  precision mediump float;\nFollowed by the uniform/varying declarations, then `void main() { ... }`. Snippets above are EXCERPTS only \u2014 your output is a full shader.", "response": "precision mediump float;\nuniform sampler2D u_texture;\nuniform float u_time;\nuniform vec2 u_resolution;\nvarying vec2 v_uv;\n\nvoid main() {\n  float off = 0.005;\n  float r = texture2D(u_texture, v_uv + vec2(off, 0.0)).r;\n  float g = texture2D(u_texture, v_uv).g;\n  float b = texture2D(u_texture, v_uv - vec2(off, 0.0)).b;\n  gl_FragColor = vec4(clamp(vec3(r, g, b), 0.0, 1.0), 1.0);\n}", "error": null, "durationMs": 12093, "receivedAt": 1776609729006}
